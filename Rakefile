require 'bundler'

namespace :site do
  desc "build jekyll site"
  task :build do
    sh "bundle exec jekyll build"
  end

  desc "deploy site"
  task :deploy do
    puts "not yet implemented"
  end
end